<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lgx.dao.ResumeMapper">
    <insert id="addResume">
        insert into resume(uid, major, school, education, address, experience, skill, evaluate, createTime, pid) VALUES
        (#{uid},#{major},#{school},#{education},#{address},#{experience},#{skill},#{evaluate},#{createTime},)
    </insert>

    <select id="selectResume"  resultType="com.lgx.model.Resume">
        select * from resume where uid=#{uid}
    </select>
</mapper>